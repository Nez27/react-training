import styled from 'styled-components';

const StyledRoom = styled.main`
  padding: 20px;
`;

const Room = () => {
  return (
    <StyledRoom>
      <p>Room page</p>
    </StyledRoom>
  );
};

export default Room;
