import styled from 'styled-components';

const StyledUser = styled.main`
  padding: 20px;
`;

const User = () => {
  return (
    <StyledUser>
      <p>User page</p>
    </StyledUser>
  );
};

export default User;
